# PassReset — v1.2.3 Requirements (ARCHIVED)

**Status:** ✅ All 3 requirements delivered
**Shipped:** 2026-04-14 · tag `v1.2.3`

---

## Bugs

- [x] **BUG-001**: SMTP relay certificates issued by an internal CA can be trusted via an opt-in `SmtpSettings.TrustedCertificateThumbprints` allowlist (SHA-1 or SHA-256). No silent bypass — entries must be explicitly configured. Documented in `docs/appsettings-Production.md`.
- [x] **BUG-002**: When AD rejects a password change because the domain's `minPwdAge` has not elapsed, the portal surfaces `ApiErrorCode.PasswordTooRecentlyChanged` (value 19) with a localized user-facing message (no generic "Unexpected Error"). SIEM event logged. Client mirror exists in `ClientApp/src/types/settings.ts`.
- [x] **BUG-003**: `deploy/Install-PassReset.ps1` preserves the existing IIS AppPool identity during upgrade (does not reset to `ApplicationPoolIdentity`). Documented in `UPGRADING.md`.

---

## Traceability — Final

| REQ-ID | Phase | Plan | Status |
|---|---|---|---|
| BUG-001 | Phase 01 (v1.2.3 Hotfix) | 01-01 | ✅ Complete |
| BUG-002 | Phase 01 (v1.2.3 Hotfix) | 01-02 | ✅ Complete |
| BUG-003 | Phase 01 (v1.2.3 Hotfix) | 01-03 | ✅ Complete |

**Coverage:** 3/3 requirements delivered · 0 dropped · 0 deferred

## Notes

- No requirement scope changed during execution.
- No breaking changes to `appsettings.Production.json` — operator configs from v1.2.2 continue to work unchanged.
