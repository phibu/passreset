# PassReset — v1.3.0 Requirements (ARCHIVED)

**Status:** ✅ All 5 requirements delivered
**Shipped:** 2026-04-15 · tag `v1.3.0`

---

## UX Features

- [x] **FEAT-001**: Operators can customize portal branding (company name, portal name, helpdesk website URL, helpdesk email, usage/limitations text block, logo image, favicon) via `appsettings.Production.json` → `ClientSettings`. Images served from `%ProgramData%\PassReset\brand\` via a second `UseStaticFiles` mount (upgrade-safe, `ServeUnknownFileTypes = false`). Defaults preserve v1.2.3 look. No tech-stack change.
- [x] **FEAT-002**: `ClientSettings.ShowAdPasswordPolicy` (default `false`) toggles an `AdPasswordPolicyPanel` above the new-password field showing minLength / complexity / history / ages from a RootDSE query. Fails closed (panel hidden) on null policy or endpoint error. Policy cached in-process (1h success / 60s failure, keyed by domain DN).
- [x] **FEAT-003**: `ClientSettings.ClipboardClearSeconds` (default `30`, `0` disables) clears the clipboard after the configured delay when the user used the generator — only if the clipboard content still matches the generated password (no clobbering of unrelated content). Countdown chip shows remaining seconds, warning color at ≤5s, "cleared" confirmation for 2s.
- [x] **FEAT-004**: On new-password field blur, a HIBP breach indicator displays (checking → safe / breached+count / unavailable). Debounced 400ms. Honors `FailOpenOnPwnedCheckUnavailable`. Client WebCrypto SHA-1 computes the full hash; only the 5-char hex prefix leaves the browser (k-anonymity). Server returns raw `{ suffixes, unavailable }` from the HIBP range API; client matches its full-hash suffix locally. `AbortController` cancels in-flight requests when the field value changes. New `pwned-check-window` rate-limit policy (20 req / 5 min per IP).

## Quality

- [x] **QA-001**: Automated test foundation in place — xUnit v3 backend suite covering provider logic, error mapping, SIEM syslog formatting, lockout decorator, validation, and `PasswordController` integration via `WebApplicationFactory`. Vitest + React Testing Library frontend suite covering components, hooks, and utilities. Coverlet thresholds (20% line / 20% branch baseline, to be raised). v8 thresholds (50% line / 40% branch). Reusable `.github/workflows/tests.yml` called from `ci.yml` and `release.yml` (`needs: tests` blocks publish on failure).

---

## Traceability — Final

| REQ-ID | Phase | Plan | Status |
|---|---|---|---|
| FEAT-001 | Phase 03 (v1-3-ux-features) | 03-01 | ✅ Complete |
| FEAT-002 | Phase 03 (v1-3-ux-features) | 03-02 | ✅ Complete |
| FEAT-003 | Phase 03 (v1-3-ux-features) | 03-03 | ✅ Complete |
| FEAT-004 | Phase 03 (v1-3-ux-features) | 03-04 | ✅ Complete |
| QA-001   | Phase 02 (v1-3-test-foundation) | 02-01..05 | ✅ Complete |

**Coverage:** 5/5 requirements delivered · 0 dropped · 0 deferred

## Notes

- No requirement scope changed during execution. One implementation detail deviated from the original RESEARCH/CONTEXT plan: FEAT-004's server response shape is `{ suffixes, unavailable }` rather than `{ breached, count }`. This is an intentional k-anonymity improvement — the server never learns which suffix matched.
- No breaking changes to `appsettings.Production.json` — operator configs from v1.2.3 continue to work unchanged.
- Security review surfaced 3 CodeQL alerts during PR #17 review — all fixed before merge (see `.planning/milestones/v1.3.0-ROADMAP.md` → Issues Hit During Close).
